# AI Coursework

Selected PyTorch notebooks and scripts from introductory and advanced AI coursework. Course slides, datasets, virtual environments, checkpoints and generated bulk outputs are intentionally excluded.

## Projects

| Directory | Topic |
|---|---|
| `intro-pytorch` | PyTorch fundamentals |
| `cnn-fashion-mnist` | Convolutional classification on Fashion-MNIST |
| `mlp-classification-regression` | MLP classification and regression experiments |
| `lstm-cnews` | LSTM text classification demo for Chinese news |
| `linear-autoencoder` | Linear autoencoder on handwritten digits |

## Verification status

- All five notebooks parse as valid Jupyter JSON.
- Stored notebook outputs contain no Jupyter error records; 67 of 69 code cells have execution counts.
- The standalone MLP Python script passes Python syntax parsing.
- Datasets and dependencies were not re-downloaded, so notebooks were not re-executed end-to-end during portfolio preparation.

Use a clean Python environment with PyTorch, torchvision, NumPy, pandas, scikit-learn, matplotlib and Jupyter as required by each notebook.
